// jQuery + Bootstrap 2.x — must be global
import jQuery from 'jquery';
window.jQuery = jQuery;
window.$ = jQuery;

// Bootstrap 2.x (custom build, not an npm package)
import '../../public/assets/js/libs/bootstrap.min.js';
